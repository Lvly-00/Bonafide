# Bonafide Backend

Laravel 11 REST API for the Bonafide tutoring platform.

See the [root README](../README.md) for full setup instructions, environment variables, and API reference.

## Quick Start

```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```

API runs at `http://localhost:8000`.
